import { RequestHandler } from "express";

class AudioController {
  createAudio: RequestHandler = async (req, res) => {};
}

const audioController = new AudioController();
export default audioController;
